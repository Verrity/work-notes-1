
---
### Теги

### [[System|Назад]]
### Далее
####
---

Установка
```bash unfold
wget http://wifi.eltex.loc:4567/deps/apns_install.sh
sudo chmod a+x apns_install.sh
sudo apns_install.sh
```
После установки
```bash unfold
sudo chmod a+xrw ap_esr.py
sudo reboot
```
Заменить `wireless.eltex.loc:5000/apemu:develop` на `wireless.eltex.loc:5000/apemu:test`
Заменить `enp4s0` на свой интерфейс.
Запустить
```bash unfold
sudo python3 ap_esr.py
```
